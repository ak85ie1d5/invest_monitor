{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block aside %}
    {% include 'sidebar.html.twig' %}
{% endblock %}

{% block header %}
    <h1 class="">{{ title }}</h1>
    <a href="{{ path('app_product_add') }}" class="bg-cyan-600 py-1 px-3 text-white font-bold text-sm rounded-lg">Add new product</a>
{% endblock %}

{% block body %}
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Type</th>
                <th>Direction</th>
                <th>ISIN</th>
                <th>Stop loss level</th>
                <th>End date</th>
                <th>Leverage</th>
                <th>Underlying name</th>
                <th>Underlying ISIN</th>
            </tr>
        </thead>
        <tbody>
            {% for product in products %}
                <tr>
                    <td>{{ product.name }}</td>
                    <td>{{ product.productType.value }}</td>
                    <td>{{ product.productDirection.value }}</td>
                    <td>{{ product.Isin }}</td>
                    <td>{{ product.StopLossLevel }} {{ product.stopLossCurrency.value }}</td>
                    <td>{{ product.endDate ? product.endDate|format_datetime('short') : '—' }}</td>
                    <td>{{ product.leverage }}</td>
                    <td>{{ product.underlyingName }}</td>
                    <td>{{ product.underlyingIsin }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
