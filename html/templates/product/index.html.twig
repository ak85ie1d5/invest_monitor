{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}


<div class="example-wrapper">
    {% block aside %}
        {% include 'sidebar.html.twig' %}
    {% endblock %}

    <a href="{{ path('app_product_add') }}">Add new product</a>

    <h1>{{ title }}</h1>

    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Type</th>
                <th>Direction</th>
                <th>ISIN</th>
                <th>Stop loss level</th>
                <th>End date</th>
                <th>Leverage</th>
                <th>Underlying name</th>
                <th>Underlying ISIN</th>
            </tr>
        </thead>
        <tbody>
            {% for product in products %}
                <tr>
                    <td>{{ product.name }}</td>
                    <td>{{ product.productType.value }}</td>
                    <td>{{ product.productDirection.value }}</td>
                    <td>{{ product.Isin }}</td>
                    <td>{{ product.StopLossLevel }} {{ product.stopLossCurrency.value }}</td>
                    <td>{{ product.endDate }}</td>
                    <td>{{ product.leverage }}</td>
                    <td>{{ product.underlyingName }}</td>
                    <td>{{ product.underlyingIsin }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
